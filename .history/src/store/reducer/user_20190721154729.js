import * as type from '../constant'
const initialState={
    username:'',
    gender:''
}
export default (state=initialState,action)=>{
    switch (action.type){
        case type.SET_USER:{
            return  {...state}
        }
        def
    }
}